import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.masterone.tailors',
  appName: 'Master One Tailors',
  webDir: 'www',
  android: {
    allowMixedContent: false
  }
};

export default config;
